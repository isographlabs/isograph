import type {ExtractSecondParam, RefetchQueryNormalizationArtifact} from '@isograph/react';
import { FavoritePhraseLoader as resolver } from '../../../FavoritePhrase';
export type Pet__FavoritePhraseLoader__output_type = (React.FC<ExtractSecondParam<typeof resolver>>);
