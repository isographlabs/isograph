import type {ExtractSecondParam, RefetchQueryNormalizationArtifact} from '@isograph/react';
import { CheckinDisplay as resolver } from '../../../PetCheckinsCard';
export type Checkin__CheckinDisplay__output_type = (React.FC<ExtractSecondParam<typeof resolver>>);
