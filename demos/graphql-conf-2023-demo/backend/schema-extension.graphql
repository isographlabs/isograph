extend type SetPetTaglineResponse
  @primary(path: "pet", field_map: [{ from: "id", to: "input.id" }])

extend type Mutation
  @expose_on(
    field: "set_pet_tagline"
    path: "pet"
    field_map: [{ from: "id", to: "input.id" }]
  )
