"""
Root object
"""
type Query {
  """
  Probably whoever is logged in.
  """
  current_user: User!
  users: [User!]!
  byah(foo: String!): String!
  user(id: ID!): User!
}

type User {
  id: ID!
  name: String!
  email: String!
  avatar_url: String!
  billing_details: BillingDetails!
}

type BillingDetails {
  id: ID!
  card_brand: String!
  credit_card_number: String!
  expiration_date: String!
  address: String!
}

"""
Some scalar
"""
scalar Foo
