export type Query__meNameSuccessor__rawResponse = {
  me: {
    id: string,
    name: string,
    successor?: ({
      id: string,
      successor?: ({
        id: string,
        name: string,
      } | null),
    } | null),
  },
}

