# Root things in isograph

- entrypoint
- field + optional "to X", i.e. pointers are not separate keyword
- type X, which lets you define a type of data that must be provided by pointers to client types
