name: CI

on: [push, pull_request]

jobs:
  build-compiler:
    name: Build compiler
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.72.0
          override: true
      - name: "Build Isograph with cargo"
        run: cargo build

  build-projects:
    name: Build projects
    runs-on: ubuntu-latest
    needs: [build-compiler]
    steps:
      - name: "Build project"
        run: ./target/debug/isograph_cli --config ./demos/graphql-conf-2023-demo/isograph.config.json
      - name: "Check working directory status"
        run: "./scripts/check-git-status.sh"
